APP_NAME=OCR Processing Engine
APP_VERSION=1.0.0
DEBUG=true
HOST=0.0.0.0
PORT=8001

# PostgreSQL (on Application Server 192.168.1.66)
DATABASE_HOST=192.168.1.66
DATABASE_PORT=5432
DATABASE_NAME=ocr_platform
DATABASE_USER=ocr_app
DATABASE_PASSWORD=ocr_secure_password_2024

# Redis (shared with MxA — using DIFFERENT databases to avoid collision)
# MxA uses db0 (broker) and db1 (result) — we use db2 and db3
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=2

# MinIO (shared Docker container mxa-minio — same creds, different buckets)
MINIO_ENDPOINT=127.0.0.1:9000
MINIO_ACCESS_KEY=mxaadmin
MINIO_SECRET_KEY=StrongPassword123!
MINIO_BUCKET_UPLOADS=ocr-uploads
MINIO_BUCKET_RESULTS=ocr-results
MINIO_USE_SSL=false

# Celery (Redis db2 broker, db3 result backend — isolated from MxA)
CELERY_BROKER_URL=redis://127.0.0.1:6379/2
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/3

# SSO (Keycloak on gint realm)
SSO_SERVER_URL=http://192.168.1.59
SSO_TOKEN_VERIFY_URL=http://192.168.1.59/realms/gint/protocol/openid-connect/token/introspect
SSO_JWKS_URI=http://192.168.1.59/realms/gint/protocol/openid-connect/certs

# CORS (allow PHP app server)
CORS_ORIGINS=http://192.168.1.66,http://192.168.1.90

# Tesseract
TESSERACT_CMD=/usr/bin/tesseract
